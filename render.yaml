# render.yaml
services:
  - type: web
    name: erp-company-site
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn core.wsgi:application
    envVars:
      - key: DATABASE_URL
      - key: OPENAI_API_KEY
      - key: SECRET_KEY